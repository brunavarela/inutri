import { neutralTheme, greenTheme, orangeTheme, blueTheme, darkBlueTheme, extraTheme } from './colors';

export const theme = {
  colors: {
    neutralTheme,
    greenTheme,
    orangeTheme,
    blueTheme,
    darkBlueTheme,
    extraTheme,
  },
  buttons: {
    // buttonsTheme,
  },
};
